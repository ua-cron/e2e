import { testValues } from './../common/values/values';

export const values = () => describe('Values', () => {
  testValues('ReCron');
  // testTabs('ReUnixCron');
  // testTabs('ReQuartzCron');
});
